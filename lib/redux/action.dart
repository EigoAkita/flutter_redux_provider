import 'dart:io';

class AddName {
  String name;
  AddName(this.name);
}

class AddAge {
  int age;
  AddAge(this.age);
}

class AddSex {
  int sex;
  AddSex(this.sex);
}

class AddPrefectures {
  String prefectures;
  AddPrefectures(this.prefectures);
}

class AddHobby {
  String hobby;
  AddHobby(this.hobby);
}

class AddProfileImage {
  File profileImage;
  AddProfileImage(this.profileImage);
}

class CurrentIndex {
  int index;
  CurrentIndex(this.index);
}
